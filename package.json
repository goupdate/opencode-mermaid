{
  "name": "@goupdate/opencode-mermaid",
  "version": "0.1.0",
  "description": "OpenCode plugin — render Mermaid diagrams inline in chat as SVG with collapsible source view",
  "type": "module",
  "main": "dist/index.js",
  "files": ["dist"],
  "keywords": ["opencode", "opencode-plugin", "mermaid", "diagram", "svg", "inline"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/goupdate/opencode-mermaid"
  },
  "dependencies": {
    "beautiful-mermaid": "^0.1.3"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=0.13.7"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.2.26",
    "@types/bun": "latest",
    "tsup": "^8.5.1",
    "typescript": "^5"
  },
  "scripts": {
    "build": "tsup index.ts --format esm --out-dir dist --clean",
    "prepublishOnly": "npm run build",
    "test": "bun test",
    "test:watch": "bun test --watch"
  }
}